import React from 'react';
import LandingPage from './components/LandingPage'; // Import the revamp component
import './App.css'; // Ensure your Tailwind directives are in here

function App() {
  return (
    <div className="App">
      {/* Calling the LandingPage component here 
          makes it the first thing users see.
      */}
      <LandingPage />
    </div>
  );
}

export default App;